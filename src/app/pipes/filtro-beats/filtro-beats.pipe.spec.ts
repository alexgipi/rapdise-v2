import { FiltroBeatsPipe } from './filtro-beats.pipe';

describe('FiltroPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroBeatsPipe();
    expect(pipe).toBeTruthy();
  });
});
