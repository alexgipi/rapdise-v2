import { FiltroBatallasPipe } from './filtro-batallas.pipe';

describe('FiltroPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroBatallasPipe();
    expect(pipe).toBeTruthy();
  });
});
